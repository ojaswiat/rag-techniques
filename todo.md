Clarifications:
1. Phase 2 and Phase 3: If I augment the judge with the search tool, then the judge can find the answers correctly and human can evaluate it better.
2. Given the use case, will SQLite perform better of can it be switched to a different database on a local server such as Postgres?

3. Write phased plan, `PhasePlan.md` with claude for each phase.
4. Design phase-by-phase architecture using `PhasePlan.md` file - keep it detailed and mention all the requirements.
5. Create diagrams using draw.io MCP.
6. Write the proposal using the phased plan.